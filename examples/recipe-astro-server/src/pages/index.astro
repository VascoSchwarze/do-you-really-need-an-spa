---
// Astro's i18n routing (prefixDefaultLocale + redirectToDefaultLocale) turns
// this into an automatic redirect to /de/. Unlike recipe-astro (output:
// 'static', where this becomes a prebuilt HTML file with a meta-refresh),
// this page is on-demand by default under output: 'server', so the redirect
// is a real HTTP 301/302 issued per request.
---
